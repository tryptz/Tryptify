# Claude Code project instructions

## Git commit identity (mandatory)

- Author AND committer for every commit must be:
  `tryptz <xboxps3456felix@gmail.com>`
  Run at session start before committing:
  `git config user.name "tryptz" && git config user.email "xboxps3456felix@gmail.com"`
- Do NOT add `Co-Authored-By: Claude` (or any co-author trailer),
  `Claude-Session:` lines, "Generated with Claude Code" footers, or any
  other AI attribution to commit messages. Plain conventional commit
  messages only.
