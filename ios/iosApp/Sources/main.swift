import UIKit

// Entry point. `main.swift` is the only file allowed top-level expressions.
UIApplicationMain(
    CommandLine.argc,
    CommandLine.unsafeArgv,
    nil,
    NSStringFromClass(AppDelegate.self)
)
